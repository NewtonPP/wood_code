# Python 3.6 compatible dependencies (Jetson Nano / JetPack 4.6, system Python 3.6).
# Each pin is the last release of that package that still supports Python 3.6.
#
# IMPORTANT — provided by JetPack on the device, do NOT pip install on the Nano:
#   tensorrt, pycuda           (no PyPI support on aarch64 / Python 3.6)
#   opencv-python (cv2)        (no cp36 aarch64 wheels — use the system cv2)
# Create the venv with `--system-site-packages` so those system packages are
# visible, and upgrade pip first so it respects requires_python:
#   python3 -m pip install --upgrade "pip<22" "setuptools<60"

# --- Web backend (FastAPI + ASGI server) ---
fastapi==0.83.0
starlette==0.19.1
pydantic==1.9.2
uvicorn==0.16.0
anyio==3.6.2
asgiref==3.4.1
click==8.0.4
h11==0.13.0
idna==3.10
python-dotenv==0.20.0
typing_extensions==4.1.1

# --- Scientific ---
# numpy is pip-installable on Python 3.6 aarch64 (last 3.6 release, cp36 wheels).
# On the Jetson it may already come from JetPack; --system-site-packages makes it
# visible. cv2/OpenCV is intentionally NOT listed (see note above).
numpy==1.19.5
