# Cloud inference-service dependencies.
#
# numpy + opencv do the frame decode and post-processing (woodchip_core).
# onnxruntime (CPU) serves the exported DETR model (INFER_BACKEND=onnx);
# switch to onnxruntime-gpu on a GPU host.

fastapi>=0.110
uvicorn[standard]>=0.27
numpy>=1.26
opencv-python-headless>=4.8
onnxruntime>=1.17
